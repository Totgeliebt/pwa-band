import React from 'react';
import {StyledButton} from "../styles/StyledButton";

const Button = () => {
  return (
    <StyledButton>
     + Добавить
    </StyledButton>
  );
};

export default Button;


